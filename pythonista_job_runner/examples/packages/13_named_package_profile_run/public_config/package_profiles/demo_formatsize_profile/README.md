Version: 0.6.13-examples.1

# Demo formatsize profile

This profile is used by example `13_named_package_profile_run`.

Copy this whole folder into the add-on public config area under `package_profiles/`, copy the matching wheel into `wheel_uploads/`, then either build the profile from the Packages view or let the first profile-mode run build it on demand.
