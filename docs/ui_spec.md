Version: 0.1.0

# UI specification (draft)

Target: modern iOS app feel with tab navigation, large titles, searchable lists, drill-down detail screens, and Share Sheet deep linking.

Implementation direction:
- UIKit via `objc_util` (recommended)
- console fallback always available
