# 2026-03-12 pass2 mobile screenshots

This directory intentionally tracks only metadata for the pass2 mobile UI audit.

The original PNG screenshots were not committed because the PR pipeline used for this repository rejects binary file uploads.

Screens captured for this pass:
- `after_jobs_mobile.png`
- `after_detail_mobile.png`
- `after_advanced_mobile.png`

If needed, regenerate them in a local run and attach them to the pull request description instead of committing the binaries.
