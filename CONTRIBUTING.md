# Contributing

- Use pull requests (even for small changes).
- Keep changes focused.
- Bump add-on version in `pythonista_job_runner/config.yaml` when behaviour changes.
- Update `pythonista_job_runner/CHANGELOG.md`.
- Do not commit tokens or secrets.
